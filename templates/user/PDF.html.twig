<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Détails du Profil</title>
    <style>
        body {
            font-family: "Arial", sans-serif;
            font-size: 14px;
            background-color: #f4f4f4;
            color: #333;
            margin: 20px;
            padding: 20px;
        }
        .container {
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            margin: auto;
            border: 2px solid #0073e6;
        }
        h1 {
            text-align: center;
            color: #0073e6;
            border-bottom: 3px solid #0073e6;
            padding-bottom: 10px;
        }
        .peinture {
            text-align: center;
            padding: 15px;
            border-radius: 8px;
        }
        .peinture h2 {
            color: #444;
            font-size: 20px;
            margin-bottom: 10px;
        }
        .peinture p {
            font-size: 16px;
            margin: 5px 0;
        }
        .peinture img {
            width: 80%;
            max-width: 400px;
            height: auto;
            display: block;
            margin: 15px auto;
            border: 5px solid #ccc;
            border-radius: 8px;
        }
        .footer {
            text-align: center;
            font-size: 12px;
            color: #777;
            margin-top: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Détails du Profil</h1>

        <div class="peinture">
            <h2>{{ user.name }}  {{ user.lastname }}</h2>
            <h2><b>Email :</b>{{ user.email }}</h2>
            {# <p><b>Type :</b> {{ user.email ? user.email : 'Non défini' }}</p> #}
            <p><b>Date de création :</b> {{ user.dateCreation ? user.dateCreation|date('d-m-Y') : 'Non définie' }}</p>

                
            {% if imageBase64 %}
                <img src="{{ imageBase64 }}">
            {% else %}
                <p style="color: red; font-weight: bold;">Aucune image disponible.</p>
            {% endif %}
        </div>  

        <p class="footer">© 2025 - Généré par Artisfera</p>
    </div>
</body>
</html>